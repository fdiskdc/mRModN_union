<view class="card {{focused ? 'focused' : ''}}">
  <view class="header"><view><text class="label">Sequence {{index + 1}}</text><text class="length">{{summary.length || 0}} nt</text></view><text wx:if="{{removable}}" class="remove" bindtap="onRemove">Remove</text></view>
  <textarea class="input" value="{{value}}" placeholder="Paste an RNA sequence containing only A/C/G/U/T/N" maxlength="-1" auto-height="{{false}}" bindinput="onInput" bindfocus="onFocus" bindblur="onBlur"></textarea>
  <view class="meta"><text class="status {{validation.valid ? 'valid' : ''}}">{{validation.message}}</text><text>GC {{summary.gc || 0}}%</text></view>
</view>
