<view class="tabs safe-bottom">
  <view class="row">
    <view wx:for="{{tabs}}" wx:key="value" class="tab {{value === item.value ? 'active' : ''}}" data-value="{{item.value}}" bindtap="onTap">
      <text>{{item.label}}</text>
    </view>
  </view>
</view>
